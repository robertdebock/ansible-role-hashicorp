---
- name: Verify
  hosts: all
  become: yes
  gather_facts: yes

  tasks:
    - name: check if product is installed
      command: "{{ item }} --version"
      args:
        creates: nothing
      loop:
        - consul
        - nomad
        - packer
        - terraform
        - vagrant
        - vault
